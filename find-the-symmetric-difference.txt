** start of script.js **

function sym(...args) {
  return Array.from(new Set(args.reduce((arr1, arr2) => arr1.filter((num) => !arr2.includes(num)).concat(arr2.filter((num) => !arr1.includes(num)))).sort((a,b) => a - b)))
}

sym([1, 2, 3], [5, 2, 1, 4]);

** end of script.js **

